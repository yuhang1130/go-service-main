# Maintain a directly usable Service Repository template

The repository itself is the reusable starting point: it contains concrete Role entry points and shared infrastructure without requiring a generator before business development can begin. A copied repository owns and evolves all source code independently; template updates are adopted deliberately rather than pushed into existing services by a generator.
